<%= form_for(@heatmap) do |f| %>
  <% if @heatmap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heatmap.errors.count, "error") %> prohibited this heatmap from being saved:</h2>

      <ul>
      <% @heatmap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
