<%= form_for(@heatmap_point) do |f| %>
  <% if @heatmap_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heatmap_point.errors.count, "error") %> prohibited this heatmap_point from being saved:</h2>

      <ul>
      <% @heatmap_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
